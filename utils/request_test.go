package utils

import (
	"testing"
	// "work/clodeo/clodeo-go-libraries"
)

func TestRequest(t *testing.T) {
	// req := libraries.Request("https://httpbin.org")

	// resp, _, _ := req.Get([]string{"get"}, nil).End()

	// assert.Equal(t, 200, resp.StatusCode)
}
